{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "$ref": "#/definitions/ElysStaked",
    "definitions": {
        "ElysStaked": {
            "properties": {
                "address": {
                    "type": "string"
                },
                "amount": {
                    "type": "string"
                }
            },
            "additionalProperties": false,
            "type": "object",
            "title": "Elys Staked",
            "description": "Elys staked amount is tracked because EdenBoost has to be burnt when unstake ELYS event happens, and there's no way to track staked amount change from staking hook and this struct is added."
        }
    }
}