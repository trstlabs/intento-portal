{
  "$ref": "#/definitions/MsgInstantiateContract",
  "definitions": {
    "MsgInstantiateContract": {
      "properties": {
        "sender": {
          "type": "string"
        },
        "codeHash": {
          "type": "string"
        },
        "codeId": {
          "type": "string"
        },
        "contractId": {
          "type": "string",
          "description": "contract_id is a unique name for the contract"
        },
        "msg": {
          "type": "string",
          "description": "msg instantiates a new contract instance",
          "format": "binary",
          "binaryEncoding": "base64"
        },
        "autoMsg": {
          "type": "string",
          "description": "auto_msg is a message sent after a set period of time",
          "format": "binary",
          "binaryEncoding": "base64"
        },
        "funds": {
          "items": {
            "$ref": "#/definitions/cosmos.base.v1beta1.Coin"
          },
          "type": "array"
        },
        "duration": {
          "type": "string",
          "description": "duration defines the time that the code should run for"
        },
        "startDurationAt": {
          "type": "string"
        },
        "interval": {
          "type": "string",
          "description": "interval defines the interval between auto_msg calls"
        },
        "callbackSig": {
          "type": "string",
          "description": "for contracts calling other contracts",
          "format": "binary",
          "binaryEncoding": "base64"
        },
        "owner": {
          "type": "string",
          "description": "for contracts instantiating on behalf of a user"
        }
      },
      "additionalProperties": true,
      "type": "object",
      "title": "Msg Instantiate Contract"
    },
    "cosmos.base.v1beta1.Coin": {
      "properties": {
        "denom": {
          "type": "string"
        },
        "amount": {
          "type": "string"
        }
      },
      "additionalProperties": true,
      "type": "object",
      "title": "Coin",
      "description": "Coin defines a token with a denomination and an amount. NOTE: The amount field is an Int which implements the custom method signatures required by gogoproto."
    }
  }
}
